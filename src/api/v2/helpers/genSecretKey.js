const crypto = require('crypto');

// const keyAccessToken = crypto.randomBytes(32).toString('hex');
// const keyRefreshToken = crypto.randomBytes(32).toString('hex');
// const keyAuthorizationCode = crypto.randomBytes(32).toString('hex');

// console.table({keyAccessToken, keyRefreshToken, keyAuthorizationCode});
console.table({ keyAuthorizationCode });
